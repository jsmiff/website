import React from 'react';
import { Section } from '../ui/Section';
import { Page } from '../ui/Page';

const Index: React.FC = () => {
  return (
    <Page>
      <Section p={1}>json.nyc</Section>
    </Page>
  );
};

export default Index;
